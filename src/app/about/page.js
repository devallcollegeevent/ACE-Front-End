/**
 * Route component for "/about".
 * Renders the client-side AboutPage component.
 * Keep this module minimal — no data fetching or side effects here.
 */
import AboutPage from "./AboutPage";

export default function Page() {
  // Render the About page component
  return <AboutPage />;
}
